import { Canvas, Meta, Story } from '@storybook/addon-docs';

import HeroHeader from "./HeroHeader";

<Meta title="Components/organisms/HeroHeader" component={HeroHeader} />

export const Template = (args) => <HeroHeader {...args} />;

# HeroHeader

The `HeroHeader` component is a text component used for reading headings

<Canvas>
  <Story
    name="HeroHeader"
    args={{
      heading: 'Heading',
      subheading: 'Sub heading',
      showLearnMore: true,
      isLarge: false,
      image: <img src="https://via.placeholder.com/300" />
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

